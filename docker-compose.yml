services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: maimai
      POSTGRES_PASSWORD: maimai
      POSTGRES_DB: maimai_dev
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:

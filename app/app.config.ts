export default defineAppConfig({
  ui: {
    page: {
      slots: {
        // left: 'lg:col-span-3',
        // center: 'lg:col-span-6',
        right: 'lg:col-span-3 order-first lg:order-last',
      },
    },
  },
});
